# Placeholder for scripts/install-disk.sh — overwritten on the installed
# system with machine-specific settings (hostname, bootloader,
# stateVersion). Committed empty so `nixosConfigurations.omahab-installed`
# evaluates before any install exists.
{ ... }:

{ }
