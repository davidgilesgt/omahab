# Placeholder for scripts/install-disk.sh — overwritten on the installed
# system with the machine's real hardware-configuration.nix content.
# Committed empty so `nixosConfigurations.omahab-installed` evaluates
# (and `nix flake check` stays green) before any install exists.
{ ... }:

{ }
