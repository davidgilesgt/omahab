# Forgejo — curated Omahab bundle
# Private by default. Native OIDC to Pocket ID. Postgres dump required for consistent backup.
name: omahab-forgejo
networks:
  omahab:
    name: omahab
    external: true
services:
  forgejo:
    image: codeberg.org/forgejo/forgejo@sha256:${FORGEJO_DIGEST:?pinned digest required}
    restart: unless-stopped
    depends_on:
      forgejo-postgres:
        condition: service_healthy
      forgejo-redis:
        condition: service_healthy
    expose:
      - "3000"
      - "22"
    environment:
      USER_UID: "1000"
      USER_GID: "1000"
      FORGEJO__database__DB_TYPE: postgres
      FORGEJO__database__HOST: forgejo-postgres:5432
      FORGEJO__database__NAME: forgejo
      FORGEJO__database__USER: forgejo
      FORGEJO__database__PASSWD_FILE: /run/secrets/forgejo_db_password
      FORGEJO__security__SECRET_KEY_FILE: /run/secrets/forgejo_secret_key
      FORGEJO__security__INSTALL_LOCK: "true"
      FORGEJO__service__DISABLE_REGISTRATION: "true"
      FORGEJO__server__ROOT_URL: "https://git.${DOMAIN:?domain required}/"
      FORGEJO__server__SSH_DOMAIN: "git.${DOMAIN}"
    secrets:
      - forgejo_db_password
      - forgejo_secret_key
    volumes:
      - forgejo_data:/data
      - /etc/omahab/forgejo/app.ini:/data/custom/conf/app.ini:ro
    networks:
      - omahab
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://127.0.0.1:3000/-/healthz || curl -f http://127.0.0.1:3000/-/healthz || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    # No ports, no docker socket

  forgejo-postgres:
    image: docker.io/library/postgres@sha256:${POSTGRES_DIGEST:?pinned digest required}
    restart: unless-stopped
    expose:
      - "5432"
    environment:
      POSTGRES_DB: forgejo
      POSTGRES_USER: forgejo
      POSTGRES_PASSWORD_FILE: /run/secrets/forgejo_db_password
      PGDATA: /var/lib/postgresql/data/pgdata
    secrets:
      - forgejo_db_password
    volumes:
      - forgejo_postgres_data:/var/lib/postgresql/data
    networks:
      - omahab
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U forgejo -d forgejo || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  forgejo-redis:
    image: docker.io/library/redis@sha256:${REDIS_DIGEST:?pinned digest required}
    restart: unless-stopped
    expose:
      - "6379"
    command: ["redis-server", "--appendonly", "yes", "--save", "60", "1"]
    volumes:
      - forgejo_redis_data:/data
    networks:
      - omahab
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

secrets:
  forgejo_db_password:
    file: /var/lib/omahab/secrets/forgejo_db_password
  forgejo_secret_key:
    file: /var/lib/omahab/secrets/forgejo_secret_key
volumes:
  forgejo_data:
    name: forgejo_data
  forgejo_postgres_data:
    name: forgejo_postgres_data
  forgejo_redis_data:
    name: forgejo_redis_data
